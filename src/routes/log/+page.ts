// Directory-shaped output (log/index.html) — see contact/+page.ts: the static
// serving plane has no extension rewriting, so nested pages prerender as
// directories.
export const trailingSlash = 'always';
