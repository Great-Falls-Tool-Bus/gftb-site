# Claude — GFTB public microsite

Read `AGENTS.md` first. Use `just <recipe>` for all operations and keep the
site static: no backend, auth, payments, mail mutation, private content, or
infrastructure apply logic. Skeleton 5.0.0 is exact-pinned for this spoke.

Public daily logs use checked-in `.svx` and the strict metadata shape documented
in `AGENTS.md`; never place internal tracker, PR, SHA, repo, member, or private
location data on the rendered surface.
